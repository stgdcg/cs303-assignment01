#pragma once
#include "array.h"
#include <iostream>

using namespace std;

// Helper function to input a file into an array
void input_file(string file, array &arr);

// Helper function to prompt the user and return the value entered
char prompt_options();
